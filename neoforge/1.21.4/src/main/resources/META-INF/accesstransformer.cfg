# Backup AT: NeoForge's built-in AT already widens BlockEntitySupplier, but we keep
# this in case some older NeoForge build did not include it. The public varargs
# constructor BlockEntityType(BlockEntitySupplier, Block...) needs no AT.
public net.minecraft.world.level.block.entity.BlockEntityType$BlockEntitySupplier
